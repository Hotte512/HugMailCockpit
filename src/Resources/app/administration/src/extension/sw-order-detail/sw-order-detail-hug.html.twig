{% block sw_order_detail_content_tabs_extension %}
    {% parent %}

    <sw-tabs-item
        v-if="hugMailTabVisible"
        class="hug-mail-cockpit__tab"
        :route="{ name: 'sw.order.detail.hugMails', params: { id: $route.params.id } }"
        :title="$tc('hug-mail-cockpit.tab.label')"
    >
        {{ $tc('hug-mail-cockpit.tab.label') }}
    </sw-tabs-item>
{% endblock %}
